// changing single value define will have no effect
// its a single value so if you change its value, you change
// its location (NoNo).  Unlike an object, you can change the interval
// values of an object and the object location stays the same.
define(5);